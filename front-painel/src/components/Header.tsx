import "../styles/layout.css";

const Header = () => {
  return (
    <header className="header">
      <span>Dashboard</span>

      <div className="header-user">
        <span>Admin</span>
      </div>
    </header>
  );
};

export default Header;
